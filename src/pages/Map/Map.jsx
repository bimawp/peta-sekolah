import React from 'react';
import styles from './Map.module.css';

const Map = () => {
  return (
    <div className={styles.map}>
      <h1>Peta Sekolah</h1>
      {/* Tambahkan peta interaktif menggunakan Leaflet atau Google Maps */}
    </div>
  );
};

export default Map;
